# Service configuration with embedded secrets — detect-secrets should catch these
services:
  database:
    host: db.internal
    port: 5432
    username: app_user
    password: "YamlP@ssw0rd!2024"

  cache:
    url: redis://:redis_yaml_s3cr3t@cache.internal:6379

  email:
    smtp_host: smtp.example.com
    smtp_password: "mail_5mtp_yaml_p@ss"
    api_key: "sk-proj-yaml-example-key-12345"

  monitoring:
    datadog_api_key: "abc123def456ghi789jkl012mno345pqr"

  auth:
    jwt_secret: "jwt-yaml-secret-key-prod-2024"
